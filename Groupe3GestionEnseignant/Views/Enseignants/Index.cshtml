@model IEnumerable<Groupe3GestionEnseignant.Models.ENTITE.Enseignant>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prenom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MotPasse)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MotPasse)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEnseignant">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEnseignant">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEnseignant">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
