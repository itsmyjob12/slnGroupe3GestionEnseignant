@model IEnumerable<Groupe3GestionEnseignant.Models.ENTITE.Absence>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Jour)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Motif)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Matricule)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Jour)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Motif)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Matricule)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAbscent">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAbscent">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAbscent">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
